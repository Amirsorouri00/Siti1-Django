from django.conf.urls import url
from comment_app.views.rest.views import NewsViewSet, CommentViewSet

urlpatterns = [
    # url(r'^news/$', NewsViewSet.as_view(), name='commentapp-news'),
    # url(r'^comment/$', CommentViewSet.as_view(), name='commentapp-comment'),

]